---
import AppLayout from "../layouts/AppLayout.astro"
import MinistryPage from "../react/pages/ministry/[slug].jsx"

export const prerender = false

const { slug } = Astro.params
---

<AppLayout title="Ministry | MyLatinMass.com">
  <MinistryPage slug={slug} client:only="react" />
</AppLayout>
